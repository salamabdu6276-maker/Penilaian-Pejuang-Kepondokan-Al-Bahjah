--- src/components/Dashboard.tsx
+++ src/components/Dashboard.tsx
@@ -581,8 +581,8 @@
       {/* WEEKLY TREND & CALENDAR SECTION */}
       <div className="grid grid-cols-1 lg:grid-cols-12 gap-6">
         
-        {/* Weekly Trend Chart (8 Cols) */}
-        <div className="lg:col-span-7 bg-white p-6 rounded-2xl border border-slate-200 shadow-xs space-y-4">
+        {/* Weekly Trend Chart (12 Cols) */}
+        <div className="lg:col-span-12 bg-white p-6 rounded-2xl border border-slate-200 shadow-xs space-y-4">
           <div className="flex items-center justify-between">
             <div>
               <h3 className="text-base font-bold text-slate-900 flex items-center gap-2">
@@ -624,53 +624,8 @@
             </div>
           </div>
         </div>
-
-        {/* INTERACTIVE HIJRI & MASEHI CALENDAR (5 Cols) */}
-        <div className="lg:col-span-5 bg-slate-900 text-white p-6 rounded-2xl shadow-md border border-slate-800 space-y-4">
-          <div className="flex items-center justify-between border-b border-slate-800 pb-3">
-            <div>
-              <h3 className="text-base font-bold text-amber-300 flex items-center gap-2">
-                <Moon className="w-5 h-5 text-amber-400" />
-                Kalender Hijriyah & Masehi
-              </h3>
-              <p className="text-xs text-slate-400">Klik tanggal untuk melihat pejuang & kategori tertinggi</p>
-            </div>
-            <span className="text-xs font-semibold bg-amber-500/20 text-amber-300 border border-amber-500/30 px-2 py-0.5 rounded">
-              Otomatis Up to Date
-            </span>
-          </div>
-
-          {/* Mini Calendar Grid (June 30 Days sample) */}
-          <div className="grid grid-cols-7 gap-1.5 text-center text-xs">
-            {['Ahd', 'Sen', 'Sel', 'Rab', 'Kam', 'Jum', 'Sab'].map((d, i) => (
-              <div key={i} className="font-bold text-amber-400/80 py-1">{d}</div>
-            ))}
-            {Array.from({ length: 30 }, (_, i) => i + 1).map((dayNum) => {
-              const testDate = new Date(selectedYear, selectedMonth - 1, dayNum);
-              const hijri = getHijriDate(testDate);
-              const dateInfo = getTopPejuangForDate(dayNum);
-              const hasData = dateInfo.topScore > 0;
-
-              return (
-                <button
-                  key={dayNum}
-                  onClick={() => setActiveCalendarDate(dayNum)}
-                  className={`p-1.5 rounded-lg text-left transition-all relative border ${
-                    hasData
-                      ? 'bg-emerald-900/60 border-emerald-500/80 text-white hover:bg-emerald-800'
-                      : 'bg-slate-800/60 border-slate-700/60 text-slate-300 hover:bg-slate-700'
-                  }`}
-                >
-                  <div className="flex items-center justify-between">
-                    <span className="font-bold text-xs">{dayNum}</span>
-                    <span className="text-[9px] text-amber-400/90 font-medium">{hijri.day}</span>
-                  </div>
-                  {hasData && (
-                    <div className="mt-1 flex items-center justify-between text-[9px] text-emerald-300 font-bold">
-                      <span>{dateInfo.topScore}%</span>
-                      <span className="bg-emerald-500/30 px-1 rounded text-[8px]">Kat {dateInfo.topCategory}</span>
-                    </div>
-                  )}
-                </button>
-              );
-            })}
-          </div>
-          <p className="text-[11px] text-slate-400 italic text-center">
-            Penghitungan kalender mengacu pada kalender Al-Bahjah yang disinkronisasi.
-          </p>
-        </div>
       </div>
