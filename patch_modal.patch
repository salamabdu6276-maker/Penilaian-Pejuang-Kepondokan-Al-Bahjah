--- src/components/Dashboard.tsx
+++ src/components/Dashboard.tsx
@@ -1006,44 +1006,53 @@
             <div className="flex justify-between items-center border-b border-slate-100 pb-3">
               <div>
                 <h3 className="font-bold text-slate-900 text-base">
-                  Detail Performa Tanggal {activeCalendarDate} {GREGORIAN_MONTHS_ID[selectedMonth - 1]} {selectedYear}
+                  Detail Performa Tanggal {activeCalendarDate.dayNum} {GREGORIAN_MONTHS_ID[selectedMonth - 1]} {selectedYear}
                 </h3>
                 <p className="text-xs text-amber-700 font-semibold">
-                  Kalender Hijriyah: {getHijriDate(new Date(selectedYear, selectedMonth - 1, activeCalendarDate)).formatted}
+                  Kalender Hijriyah: {new Intl.DateTimeFormat('id-ID-u-ca-islamic', { day: 'numeric', month: 'long', year: 'numeric' }).format(new Date(selectedYear, selectedMonth - 1, activeCalendarDate.dayNum))}
                 </p>
               </div>
               <button 
                 onClick={() => setActiveCalendarDate(null)}
                 className="text-slate-400 hover:text-slate-600 p-1 rounded-lg"
               >
                 <X className="w-5 h-5" />
               </button>
             </div>
 
             {(() => {
-              const info = getTopPejuangForDate(activeCalendarDate);
+              const info = getTop5PejuangForDate(activeCalendarDate.dayNum);
               return (
-                <div className="space-y-3 py-2">
-                  <div className="bg-emerald-50 border border-emerald-200 rounded-xl p-4 flex items-center space-x-4">
-                    <div className="w-12 h-12 rounded-full bg-emerald-200 text-emerald-900 font-bold flex items-center justify-center overflow-hidden border border-emerald-300 text-lg">
-                      {info.topFoto ? (
-                        <img src={info.topFoto} alt={info.topName} className="w-full h-full object-cover" />
-                      ) : (
-                        info.topName.charAt(0)
-                      )}
-                    </div>
-                    <div>
-                      <span className="text-[10px] uppercase font-bold text-emerald-700 tracking-wider">Pejuang Nilai Tertinggi</span>
-                      <h4 className="font-bold text-slate-900 text-sm">{info.topName}</h4>
-                      <p className="text-xs text-emerald-800 font-semibold mt-0.5">
-                        Persentase: <span className="text-base font-black">{info.topScore}%</span>
-                      </p>
-                    </div>
-                  </div>
-
-                  <div className="grid grid-cols-2 gap-2 text-xs">
-                    <div className="p-3 bg-slate-50 rounded-lg border border-slate-200">
-                      <span className="text-slate-500 block">Kategori Kegiatan</span>
-                      <span className="font-bold text-slate-800 text-sm">Kategori {info.topCategory}</span>
-                    </div>
-                    <div className="p-3 bg-slate-50 rounded-lg border border-slate-200">
-                      <span className="text-slate-500 block">Ceklis Terisi</span>
-                      <span className="font-bold text-slate-800 text-sm">{info.totalChecks} Aktivitas</span>
-                    </div>
-                  </div>
+                <div className="space-y-3 py-2 max-h-[60vh] overflow-y-auto pr-1">
+                  <div className="text-sm font-semibold text-slate-800 mb-2 border-b border-slate-100 pb-2">
+                    Top 5 Pejuang (Total Ceklis Keseluruhan: {info.totalChecks})
+                  </div>
+                  {info.top5.length > 0 ? (
+                    info.top5.map((p, idx) => (
+                      <div key={p.pejuangId} className={`flex items-center space-x-3 p-3 rounded-xl border ${idx === 0 ? 'bg-emerald-50 border-emerald-200' : 'bg-slate-50 border-slate-200'}`}>
+                        <div className="flex-shrink-0 w-6 font-bold text-slate-400 text-center">{idx + 1}</div>
+                        <div className={`w-10 h-10 rounded-full font-bold flex items-center justify-center overflow-hidden border text-sm ${idx === 0 ? 'bg-emerald-200 text-emerald-900 border-emerald-300' : 'bg-slate-200 text-slate-700 border-slate-300'}`}>
+                          {p.fotoUrl ? (
+                            <img src={p.fotoUrl} alt={p.pejuangNama} className="w-full h-full object-cover" />
+                          ) : (
+                            p.pejuangNama.charAt(0)
+                          )}
+                        </div>
+                        <div className="flex-1 min-w-0">
+                          <h4 className="font-bold text-slate-900 text-sm truncate">{p.pejuangNama}</h4>
+                          <div className="flex items-center text-[10px] text-slate-500 gap-2 mt-0.5">
+                            <span className="font-bold text-emerald-600">{p.percentage}%</span>
+                            <span>•</span>
+                            <span>Kat {p.kategori}</span>
+                          </div>
+                        </div>
+                      </div>
+                    ))
+                  ) : (
+                    <div className="text-center py-6 text-slate-400 text-sm italic">
+                      Belum ada data aktivitas untuk tanggal ini.
+                    </div>
+                  )}
                 </div>
               );
             })()}
